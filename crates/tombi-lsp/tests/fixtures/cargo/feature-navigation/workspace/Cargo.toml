[workspace]
members = [
  "consumer",
  "provider",
  "registry-consumer",
  "renamed-consumer",
  "weak-consumer"
]

[workspace.dependencies]
provider = { path = "provider", features = ["jsonschema"] }
