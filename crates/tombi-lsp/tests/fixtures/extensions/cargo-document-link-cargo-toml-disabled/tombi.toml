[extensions]
"tombi-toml/cargo" = {
  lsp = {
    document-link = {
      cargo-toml.enabled = false,
      git.enabled = true,
      path.enabled = true,
      workspace.enabled = true,
    },
  },
}
