[extensions]
"tombi-toml/cargo" = {
  lsp = {
    document-link = {
      cargo-toml.enabled = false,
      path.enabled = true,
      workspace.enabled = true,
      git.enabled = true,
    },
  },
}
