[extensions]
"tombi-toml/cargo" = {
  lsp = {
    document-link = {
      # tombi: lint.rules.deprecated.disabled = true
      cargo-toml = { enabled = true },
      crates-io.enabled = false,
      # tombi: lint.rules.deprecated.disabled = true
      path = { enabled = true },
    },
  },
}
