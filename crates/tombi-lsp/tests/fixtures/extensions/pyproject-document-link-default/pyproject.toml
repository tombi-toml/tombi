[project]
name = "pyproject-document-link-default"
version = "0.1.0"
dependencies = [
  "anyio>=4.0",
  "member",
]

[tool.uv.workspace]
members = ["member"]

[tool.uv.sources]
member = { workspace = true }
