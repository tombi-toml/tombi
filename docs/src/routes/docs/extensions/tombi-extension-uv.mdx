import { Note } from "~/components/Highlight";

# uv Extension

<Note>
This page has moved to [Pyproject Extension](/docs/extensions/tombi-extension-pyproject).
The feature set still understands uv-specific workspace metadata, but the documentation now uses the broader `pyproject.toml` name.
</Note>
