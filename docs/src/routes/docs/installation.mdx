import CodeTabs from "../../components/CodeTabs";
import { createSignal } from "solid-js";

export const [selectedMethod, setSelectedMethod] = createSignal(null);

# Installation

Choose your preferred installation method:

<InstallationMethodGrid onSelect={(method) => {
  setSelectedMethod(method);
  const element = document.getElementById(method);
  if (element) {
    element.scrollIntoView({ behavior: 'smooth', block: 'start' });
  }
}} />

## <span id="cli">CLI</span>

```sh
curl -fsSL https://tombi-toml.github.io/tombi/install.sh | sh
```

Install a specific version:

```sh
curl -fsSL https://tombi-toml.github.io/tombi/install.sh | sh -s -- --version 0.2.24
```

## <span id="python">Python</span>

### <span id="uvx">Quick Try</span>

<CodeTabs
  tabs={[
    {
      key: "uvx",
      label: "uvx",
      command: "uvx tombi",
    }
  ]}
  defaultKey="uvx"
  language="bash"
/>

### <span id="uv"><span id="poetry"><span id="hatch"><span id="pip">Project Installation</span></span></span></span>

<CodeTabs
  tabs={[
    {
      key: "uv",
      label: "uv",
      command: "uv add --dev tombi",
    },
    {
      key: "poetry",
      label: "poetry",
      command: "poetry add --dev tombi",
    },
    {
      key: "hatch",
      label: "hatch",
      command: "hatch add --dev tombi",
    },
    {
      key: "pip",
      label: "pip",
      command: "pip install tombi",
    },
  ]}
  defaultKey="uv"
  language="bash"
/>


### <span id="pipx">Global Installation</span>

<CodeTabs
  tabs={[
    {
      key: "uv",
      label: "uv",
      command: "uv tool install tombi",
    },
    {
      key: "pipx",
      label: "pipx",
      command: "pipx install tombi",
    },
  ]}
  defaultKey="uv"
  language="bash"
/>

## <span id="javascript">JavaScript</span>

### <span id="npx">Quick Try</span>

<CodeTabs
  tabs={[
    {
      key: "npx",
      label: "npx",
      command: "npx tombi",
    }
  ]}
  defaultKey="npx"
  language="bash"
/>

### <span id="npm"><span id="pnpm"><span id="yarn">Project Installation</span></span></span>

<CodeTabs
  tabs={[
    {
      key: "npm",
      label: "npm",
      command: "npm install --save-dev tombi",
    },
    {
      key: "pnpm",
      label: "pnpm",
      command: "pnpm add --save-dev tombi",
    },
    {
      key: "yarn",
      label: "yarn",
      command: "yarn add --dev tombi",
    },
  ]}
  defaultKey="npm"
  language="bash"
/>

### Global Installation

<CodeTabs
  tabs={[
    {
      key: "npm",
      label: "npm",
      command: "npm install -g tombi",
    },
    {
      key: "pnpm",
      label: "pnpm",
      command: "pnpm add -g tombi",
    },
    {
      key: "yarn",
      label: "yarn",
      command: "yarn global add tombi",
    },
  ]}
  defaultKey="npm"
  language="bash"
/>

## <span id="vscode">VSCode</span> / <span id="cursor">Cursor</span>

<LinkCard
  title="Tombi - Visual Studio Marketplace"
  description="Extension for Visual Studio Code - TOML Language Server"
  image="/vscode.svg"
  url="https://marketplace.visualstudio.com/items?itemName=tombi-toml.tombi"
/>

## <span id="openvsx">Open VSX</span>

<LinkCard
  title="Tombi - Open VSX Registry"
  description="Extension for Open VSX - TOML Language Server"
  image="/openvsx.png"
  url="https://open-vsx.org/extension/tombi-toml/tombi"
/>

## <span id="zed">Zed</span>

<LinkCard
  title="Tombi - Zed Extensions"
  description="Extension for Zed - TOML Language Server"
  image="/zed.jpeg"
  url="https://zed.dev/extensions?query=tombi&filter=language-servers"
/>

## <span id="neovim">Neovim</span>

<LinkCard
  title="Tombi - Neovim"
  description="Neovim - TOML Language Server"
  image="/neovim.png"
  url="https://github.com/neovim/nvim-lspconfig/blob/master/lsp/tombi.lua"
/>

## <span id="emacs">Emacs</span>

<LinkCard
  title="Tombi - Emacs"
  description="Emacs - TOML Language Server"
  image="/emacs.png"
  url="https://emacs-lsp.github.io/lsp-mode/page/lsp-toml-tombi/"
/>

## <span id="github-actions">GitHub Actions</span>

<LinkCard
  title="Tombi - GitHub Action"
  description="GitHub Action to install Tombi"
  image="/github-action.png"
  url="https://github.com/tombi-toml/setup-tombi"
/>
